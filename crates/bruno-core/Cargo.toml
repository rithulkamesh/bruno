[package]
name = "bruno-core"
version = "0.1.0"
edition = "2024"
description = "Bruno shared events and broadcast bus"
license = "MIT"

[dependencies]
serde = { version = "1", features = ["derive"] }
tokio = { version = "1", features = ["sync"] }
